# Go content API. Read on the server for rendering and in the browser for forms.
NEXT_PUBLIC_API_URL=https://backend.aiddev.net

# Canonical origin of this site. Open Graph images and canonical links must be
# absolute URLs, so this has to be set correctly for sharing to work.
NEXT_PUBLIC_SITE_URL=https://aiddev.net
