# Where the Go content API lives.
#
# CRA inlines REACT_APP_* at build time, so this must be set before
# "npm run build", not at runtime.
#
# Production:  https://backend.aiddev.net
# Local:       http://localhost:8080
REACT_APP_API_URL=http://localhost:8080
