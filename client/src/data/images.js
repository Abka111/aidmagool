// The header and footer are deliberately not editable from the admin panel, so
// the logo stays bundled with the site. Every other image now comes from the
// content API as an /uploads path.
import logo from '../logo.jpg';

export { logo };
